---
- name: install mongodb
  apt:
    name: "{{ item }}"
    state: present
    force: yes
  become: true
  with_items:
    - mongodb

- name: start mongodb
  service: name=mongodb state=started
  become: true

- name: wait for mongodb to become available
  wait_for:
    port: 27017
